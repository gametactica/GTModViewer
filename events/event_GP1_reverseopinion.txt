namespace = event_GP1_reverseopinion

country_event =
{
    id = event_GP1_reverseopinion.1
    title = event_GP1_reverseopinion.1.t
    desc = event_GP1_reverseopinion.1.d
    picture = ANGLICAN_SERMON_eventPicture

    hidden = yes

    mean_time_to_happen = {months = 1} 

    trigger =
    {
        ai = yes
        is_emperor = yes
        is_great_power = yes 
        NOT = {great_power_rank = 2}
    }

    immediate = 
    {
        hidden_effect = 
        {
            every_country =
            {
                limit = 
                {
                    is_part_of_hre = yes
                    OR = 
                    {
                        is_subject = no
                        is_subject_of_type = tributary_state
                    }
                }

                reverse_add_opinion = 
                {
                    who = ROOT
                    modifier = GT_GP_Emperor
                }
            }
        }
    }

    option =
    {
        name = event_GP1_reverseopinion.1.a
        
        #if event breaks with ai, try code below
        #ai_chance = {factor = 100}
    }
}